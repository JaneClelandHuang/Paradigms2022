

/**
 * interface for quack strategies.  Currently only one behavior but we could imagine more
 * for example, setVolume() or moveBeak().
 * @author Jane
 *
 */
public interface IQuackStrategy {
	public void quack();
}
