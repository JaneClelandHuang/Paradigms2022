var assert = require('assert');
describe('Math', function() {
    it('should return 9 when multiplying 3 * 3', function() {
      assert.equal(3*3, 9);
    });
	it('should return -8 when..',function() {
	  assert.equal((3-4)*8,-8);
	});
});